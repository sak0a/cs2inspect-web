/**
 * Re-export all common types from the shared directory
 *
 * This file preserves backward compatibility — existing imports from
 * '~/types/core/common' continue to work.
 */
export * from '#shared/types/common'
